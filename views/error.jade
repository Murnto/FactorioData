extends layout

block content
    .container
        h1= message
        h2= error.status
        if error.stack
            pre= error.stack

        if extended_message
            pre= extended_message
